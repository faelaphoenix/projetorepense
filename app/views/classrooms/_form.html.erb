<%= form_for(@classroom) do |f| %>
  <% if @classroom.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@classroom.errors.count, "error") %> prohibited this classroom from being saved:</h2>

      <ul>
      <% @classroom.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id %><br>
    <%= f.number_field :id %>
  </div>
  <div class="field">
    <%= f.label :students_id %><br>
    <%= f.number_field :students_id %>
  </div>
  <div class="field">
    <%= f.label :courses_id %><br>
    <%= f.number_field :courses_id %>
  </div>
  <div class="field">
    <%= f.label :entry_at %><br>
    <%= f.datetime_select :entry_at %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
